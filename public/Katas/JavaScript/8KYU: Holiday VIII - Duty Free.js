function dutyFree(normPrice, discount, hol) {
    // Get the savings first
    let totalSavings = normPrice * (discount / 100);
    // Divide holiday price by savings and round down
    return howManyBottles = Math.floor(hol / totalSavings);
}
